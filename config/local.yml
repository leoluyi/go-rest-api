dsn: "postgres://127.0.0.1/go_restful?sslmode=disable&user=postgres&password=postgres"
jwt_signing_key: "LxsKJywDL5O5PvgODZhBH12KE6k2yL8E"
# Override with APP_AUTH_USERNAME and APP_AUTH_PASSWORD env vars in production.
auth_username: "demo"
auth_password: "changeme"
# Development only — restrict to specific origins in production.
cors_allowed_origins:
  - "*"
# Database connection pool (defaults shown — override per environment).
db_max_open_conns: 25
db_max_idle_conns: 5
db_conn_max_lifetime_minutes: 5
# Graceful shutdown timeout in seconds.
shutdown_timeout_seconds: 10
